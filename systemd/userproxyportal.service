[Unit]
Description=User Proxy Portal — proxy local authentifiant
After=gnome-keyring-daemon.service network.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/local/bin/userproxyportal-proxy
Restart=on-failure
RestartSec=3

[Install]
WantedBy=graphical-session.target
